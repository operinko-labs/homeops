---
# ExternalSecret for medikabalans-admin's own Forgejo credential, so its
# InputProvider/GitRepository don't depend on the medikabalans app's
# Kustomization having reconciled first. Same Bitwarden field as the
# site's deploy-dispatch module.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: medikabalans-admin-forgejo-pat
  namespace: medikabalans
spec:
  refreshInterval: 1h
  target:
    name: medikabalans-admin-forgejo-pat
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      data:
        # Forgejo resolves the user from the token when it is sent as the
        # basic-auth password; the username only has to be non-empty.
        username: token
        password: "{{ .forgejo_dispatch_pat }}"
  data:
    - secretKey: forgejo_dispatch_pat
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: f7f2ed36-37d2-438c-86b9-4e0d99c4b763
        property: forgejo_dispatch_pat
