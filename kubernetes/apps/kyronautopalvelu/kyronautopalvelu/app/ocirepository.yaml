---
# 0.7.16 is the first chart version with initContainer.autoInstall, which the
# first start of this site depends on.
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: drupal
  namespace: kyronautopalvelu
spec:
  interval: 1h
  url: oci://harbor.vaderrp.com/operinko-labs/drupal
  ref:
    semver: ">=0.7.16"
  secretRef:
    name: harbor-pull-secret
