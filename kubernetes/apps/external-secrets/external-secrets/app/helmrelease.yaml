---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-secrets
  namespace: external-secrets
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: external-secrets
  maxHistory: 2
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  values:
    installCRDs: true
    # Two webhook replicas so the minAvailable: 1 budget in pdb.yaml can be
    # honoured during node drains; with one replica every drain of its node
    # stalls on this pod until the VM is stopped underneath it.
    webhook:
      replicaCount: 2
    # Without this the controller exports externalsecret_status_condition and
    # nothing scrapes it, so the grafanadashboard alongside this file has been
    # rendering empty and no alert on ExternalSecret health was possible.
    # Enabling the ServiceMonitor also renders the metrics Service the chart
    # otherwise leaves off — metrics.service.enabled is not needed as well.
    serviceMonitor:
      enabled: true
      interval: 1m
