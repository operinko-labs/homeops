---
# Kometa configuration
# https://kometa.wiki/en/latest/config/overview/
#
# Secrets (<<name>>) are injected from KOMETA_<NAME> env vars provided by
# the kometa-secret ExternalSecret (Kometa "Config Secrets" feature).
#
# NOTE: Library names below must exactly match the library names in Plex.

libraries:
  Movies:
    remove_overlays: false
    operations:
      # Fix/normalize metadata from TMDb
      mass_genre_update: tmdb
      mass_content_rating_update: tmdb
      mass_originally_available_update: tmdb
      mass_studio_update: tmdb
      # Uncomment to also sync ratings:
      # mass_critic_rating_update: tmdb
      # mass_audience_rating_update: tmdb
  TV Shows:
    remove_overlays: false
    operations:
      mass_genre_update: tmdb
      mass_content_rating_update: tmdb
      mass_originally_available_update: tmdb
      mass_studio_update: tmdb

settings:
  cache: true
  cache_expiration: 60
  asset_directory: /config/assets
  asset_folders: true
  show_missing: false
  show_unmanaged: true
  show_unconfigured: true
  show_filtered: false
  show_options: false
  save_report: false
  minimum_items: 1
  delete_below_minimum: false
  run_again_delay: 2
  verify_ssl: true

plex:
  url: https://plex.vaderrp.com
  token: <<plextoken>>
  timeout: 60
  clean_bundles: false
  empty_trash: false
  optimize: false

tmdb:
  apikey: <<tmdbapikey>>
  language: en
  region: FI
  cache_expiration: 60

radarr:
  url: http://radarr.media.svc.cluster.local
  token: <<radarrapikey>>
  add_missing: false
  add_existing: false
  upgrade_existing: false
  monitor: true
  availability: announced
  # Only used when add_missing is enabled — verify these match your Radarr setup
  root_folder_path: /mnt/media/Movies
  quality_profile: HD-1080p

sonarr:
  url: http://sonarr.media.svc.cluster.local
  token: <<sonarrapikey>>
  add_missing: false
  add_existing: false
  upgrade_existing: false
  monitor: all
  series_type: standard
  season_folder: true
  # Only used when add_missing is enabled — verify these match your Sonarr setup
  root_folder_path: /mnt/media/TV
  quality_profile: HD-1080p
