---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-image-reflector
  namespace: flux-system
spec:
  refreshInterval: 1h
  target:
    name: harbor-image-reflector
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: '{"auths":{"harbor.vaderrp.com":{"username":"robot$cluster-puller","password":"{{ .robot_secret }}"}}}'
  data:
    # Same robot account the Harbor config uses (harbor item, robot_secret field)
    - secretKey: robot_secret
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: 84fb75c3-a5fd-4c28-97d3-ca0a4d240ef5
        property: robot_secret
